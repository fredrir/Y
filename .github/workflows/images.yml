name: Build images
'on':
  push:
    branches:
    - main
    paths-ignore:
    - "README.md"
    - "docs/**"
concurrency:
  group: images-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  api:
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    uses: fredrir/infra/.github/workflows/build-image.yml@d9016b2889c88f1ec99bfe9d7262343a759e4b69
    with:
      image: ghcr.io/fredrir/y-api
      context: .
      dockerfile: Dockerfile
      recipe: y-api
  web:
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    uses: fredrir/infra/.github/workflows/build-image.yml@d9016b2889c88f1ec99bfe9d7262343a759e4b69
    with:
      image: ghcr.io/fredrir/y-web
      context: .
      dockerfile: Dockerfile
      recipe: y-web
      check-target: check-reports
